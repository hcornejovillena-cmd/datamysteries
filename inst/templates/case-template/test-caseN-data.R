test_that("Case N datasets keep their expected shapes", {
  # expect_equal(dim(dataset_name), c(n_rows, n_cols))
})

test_that("Case N datasets keep their expected columns", {
  # expect_named(dataset_name, c("id", "date", "value"))
})

test_that("Case N relational keys are internally consistent", {
  # expect_true(all(child_table$key %in% parent_table$key))
})

test_that("Case N mystery signals match the intended story", {
  # Verifica que el patron central del caso siga existiendo:
  # - el problema aparece donde/cuando debe aparecer;
  # - las hipotesis senuelo no explican el resultado;
  # - la causa raiz ocurre antes del deterioro observado;
  # - la decision final puede sostenerse con evidencia.
})
